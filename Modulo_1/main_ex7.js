//Utilize a sintaxe curta de objetos (Object Short Syntax) no objeto
const nome = 'Chrysler';
const idade = 20;

const usuario = {
    nome,
    idade,
    cidade: 'Rio do Sul',
};

console.log(usuario);